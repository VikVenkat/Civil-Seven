<%= form_for(@shirt) do |f| %>
  <% if @shirt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shirt.errors.count, "error") %> prohibited this shirt from being saved:</h2>

      <ul>
      <% @shirt.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
	<%= f.label :fabric %><br />
	<%= f.text_field :fabric %>
  </div>

  <div class="field">
	<%= f.label :price %><br />
	<%= f.text_field :price %>
  </div>

  <div class="field">
	<%= f.label :style_id %><br />
	<%= f.text_field :style_id %>
  </div>


  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
